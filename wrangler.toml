# Cloudflare Pages config. The site is pure static files in web/ (no build step).
# Deploy from the CLI:   npm run deploy   (runs `wrangler pages deploy web`)
# First time only:       npx wrangler login
name = "stream-processing"
pages_build_output_dir = "web"
